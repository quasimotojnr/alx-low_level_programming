#include <stdio.h>

/**
 * main - a programme that prints standard otput on terminal
 * using puts function
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char myStr[] = "\"Programming is like building a multilingual puzzle";

	puts(myStr);

return (0);
}
