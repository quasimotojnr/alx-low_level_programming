#include <stdio.h>

/**
 * mul - Calculates the products of two numbers
 * @a: First operand
 * @b: Second operand
 *
 * Return: The products of the two parameters
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
