#include <stdio.h>

/**
 * add - Makes the sum of two numbers
 * @a: First operand
 * @b: Second operand
 *
 * Return: The sum of the two parameters
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
