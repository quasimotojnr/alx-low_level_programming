#include <stdio.h>

/**
 * main - Programm printing all combination of single
 * digits
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num = 0;
	int ascii = 0;

	while (ascii < 10)
	{
		putchar(num);
		putchar(44)
		putchar(' ')
		num++;
		ascii++;
	}
	return (0);
}
